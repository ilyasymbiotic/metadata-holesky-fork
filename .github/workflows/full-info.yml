name: Get full info
on:
  push:
    branches:
      - main
jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install tsx
        run: npm install -g tsx
      - name: Generate
        run: tsx generate-full-info.ts
